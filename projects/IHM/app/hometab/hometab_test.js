'use strict';

describe('myApp.hometab module', function() {
  
  beforeEach(module('myApp.hometab'));
  
  describe('hometab controller', function(){
    
    it('should ....', inject(function($controller) {
      //spec body
      var hometabCtrl = $controller('hometabCtrl');
      expect(hometabCtrl).toBeDefined();
    }));
    
  });
});