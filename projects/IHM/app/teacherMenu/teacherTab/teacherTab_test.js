'use strict';

describe('myApp.teacherTab module', function() {
  
  beforeEach(module('myApp.teacherTab'));
  
  describe('teacherTab controller', function(){
    
    it('should ....', inject(function($controller) {
      //spec body
      var teachertabCtrl = $controller('teachertabCtrl');
      expect(teachertabCtrl).toBeDefined();
    }));
    
  });
});
